services:
  claudex-server:
    build: .
    container_name: claudex-server
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - claudex-data:/data
    environment:
      - PORT=3000

volumes:
  claudex-data:
